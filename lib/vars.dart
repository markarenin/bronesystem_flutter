const baseUrl = "http://10.0.2.2:8000";
const loginUrl = "$baseUrl/api/login/";
const servicesUrl = "$baseUrl/api/services/";
Uri baseUri = Uri.parse(baseUrl);
Uri loginUri = Uri.parse(loginUrl);
Uri servicesUri = Uri.parse(servicesUrl);
